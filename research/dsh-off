# DeepSeek Harness 一键安装助手（设计讨论稿 · 双模：离线 / 在线）

> 阶段：方案讨论。目标读者：让我和你都能看清「做给小白用的安装器」要长什么样、有多大、怎么实现。
> 结论以文档末尾「需要拍板的决策」为准，逐条确认后再进入实现。文件于 2026-09-03 更新。

## 1. 这个东西是做什么的

一个**带简单图形界面的 Windows 安装助手**：帮完全不懂编程的人，在一台新电脑上从零装好并打开 DeepSeek Harness 网页版，全程只点按钮、看提示，不看命令行。

支持两条安装路线，属于同一个程序、同一个界面：

- **离线安装（推荐，需随包离线数据）**：把近 1000 个依赖包的下载成本只付一次，之后每台电脑从本地缓存秒装，网络再差也不怕。
- **在线安装（无离线包也能跑）**：程序本体独立运行；没有随包数据时，自动从官方源 / 镜像在线下载安装。

两种模式**自动决策、可手动切换、可混用**：一份程序，带全 assets 就是完整离线盘；裸程序就是轻量在线安装器。同一份盘也服务自己：以后换电脑 / 帮同事装，双击同一个安装器即可。

## 2. 安装器长什么样（界面草图）

中文字体、大白话、大按钮，没有乱码命令行：

```
┌────────────────────────────────────────────────────┐
│   DeepSeek Harness 安装助手        v0.1            │
├────────────────────────────────────────────────────┤
│ 本工具帮你在本电脑上完成：自动安装环境 → 安装依赖   │
│ → 打开网页版。全部自动，不需要你会编程。            │
│                                                    │
│ 安装方式：                         (离线包: 已检测) │
│   ● 自动选择（推荐）                              │
│     有离线包就走离线；缺哪步，那一步自动去网上补     │
│   ○ 离线安装（不依赖网络，需随包离线数据）          │
│   ○ 在线安装（从官方源下载）                       │
│                                                    │
│   [ ✅ 一键完整安装 ]      [ 🚀 只启动网页版 ]      │
│                                                    │
│ 状态：                                             │
│  [✓] 检测 Windows …… Windows 11 64 位             │
│  [✓] 检测 Node.js …… v24.20.0（已满足，跳过）      │
│  [ ] 安装依赖(离线) …… 正在安装第 230/1001 个包     │
│  [ ] 构建程序 ……                                   │
│  [ ] 启动服务 …… http://127.0.0.1:3080             │
│                                                    │
│  ⚠ 期间若弹出「系统权限」窗口，请点【是】。          │
└────────────────────────────────────────────────────┘
```

- 「自动选择」为默认：离线数据在就用离线的，不在那一步才走网络，不会整场失败。
- 「一键完整安装」：按需执行全部步骤，已完成的不重复做（可反复点）。
- 「只启动网页版」：装好后的日常入口，秒开。
- 进度、日志、当前状态、绿勾红叉，全部中文显示在窗口里。

## 3. 它在后台替你做的六件事 + 每步的来源

| 步骤 | 做什么 | 跳过条件 | 离线来源（assets） | 在线来源（无 assets / 在线模式） |
|---|---|---|---|---|
| ① 环境自检 | 查 Windows 位数、现有 Node/pnpm 版本 | — | — | 同左 |
| ② 装 Node.js | 没 Node 或版本旧 → 安装 Node 24 LTS | 已有 22.19+/24+ | 随包 `assets/node-24-x64.msi` 静默装 | 自动下载 nodejs.org 官方 msi 再静默装 |
| ③ 准备 pnpm | 装好 pnpm（对齐项目锁定版本 11.x） | 已有 pnpm | corepack 启用 + 随包 `assets/pnpm.tgz` 离线安装 | corepack 启用，或 `npm install -g pnpm` |
| ④ 放好项目源码 | 把项目代码放到 `%USERPROFILE%\deepseek-harness` | 已存在且完整 | 解压随包 `assets/deepseek-harness-src.7z` | `git clone` 官方仓库 |
| ⑤ 装依赖（核心） | `pnpm install` | 已有 node_modules 且完整 | `--offline --store-dir <随包 pnpm-store>` 不联网 | 在线（默认官方源；可选国内镜像提速） |
| ⑥ 构建 + 启动 | `pnpm run build`（首次 5–15 分钟）→ `pnpm dsh web` | build 产物已存在（`.dsh-build` 标记） | 同左（产物在本机编译） | 同左 |

- **启动地址带一次性 token（实测）**：`dsh web` 每次生成形如 `http://127.0.0.1:3080/?token=…` 的 URL 并自动打开默认浏览器。安装助手不能假设固定裸地址；应捕获 `dsh web` 打印的 URL 展示给用户，并提示「日志里那行带 token 的地址也可手动访问」。
- 第⑥步的 build 永远在接收机本地执行、不联网——两种模式一致。
- 每步失败：中文说明 + 「复制日志」按钮，方便把报错发回我们处理。

## 4. 安装方式与「来源决策」逻辑

启动页三选一，默认「自动」：

- **自动（推荐）**：每一步单独决策——离线数据在就走离线，不在才走网络。例如有离线依赖包但没带 Node msi → 依赖离线装、Node 在线下。「部分离线」也不会白等。
- **离线**：assets 齐全才适用；缺某部分会明确提示缺在哪，由用户决定是否切在线。
- **在线**：完全忽略 assets，走官方源 / 镜像；适合「只有一个程序文件」的场景。

自动模式的实际含义：assets 完整 = 全离线零网络；assets 缺失 = 全在线；assets 部分缺失 = 能离线的离线、缺的补网。

## 5. 为什么离线包是「依赖缓存」而不是「直接把 node_modules 打包」

这是方案最需要想清楚的技术点：

- pnpm 的 `node_modules` 大量使用**硬链接 / 符号链接**省空间。用 zip 分发，链接会碎：解压后要么体积爆炸，要么直接跑不起来。
- pnpm 自带**内容寻址缓存 store**：文件按内容哈希存放，没有链接弱点，zip/7z 压缩解压后依然有效。
- 所以打包机把依赖完整下载一次 → 打包 **store 目录** → 接收机用 `pnpm install --offline --store-dir <随包store>` 安装，由 pnpm 在目标机重建正确链接。这是离线分发依赖的可靠姿势。

`pnpm store path` 可查 store 位置。**本机现状（2026-09-03）：`pnpm install` 已完整成功**——store 已含全部所需包（含 codex 之类大件），离线打包的硬前提已满足。

## 6. 两种分发形态的体积对比

| 形态 | 组成 | 体积 | 使用场景 |
|---|---|---|---|
| **在线版（瘦）** | 只有 installer.exe | 10–30MB | 有网的机器；可最先发给别人 |
| **离线版（胖）** | installer.exe + assets 全套 | ~1–2GB | 网差 / 离线机器；一次下载，处处免等 |

installer.exe 本体同一份，运行时扫描 `assets/`：有则提供离线能力，无则纯在线。「先瘦后胖」升级路径顺：先发瘦版让有网的人跑起来，再把离线 assets 单独补大包给网差的人。

## 7. 技术选型（需要拍板的第一件事）

| 方案 | 优点 | 缺点 | 适合 |
|---|---|---|---|
| **A. Python 3.11+ Tkinter，PyInstaller 打包 exe**（推荐） | UI 逻辑快、依赖轻、单文件分发；subprocess 精确控制每步命令与日志 | exe 可能被杀软报「未知发行者」；本机需先装 Python | 快速做出可用 MVP |
| B. C#/.NET WinForms/WPF | Windows 原生、UI 现代、可出签名包 | 开发链重（VS 或 dotnet SDK）；产出上百 MB | 长期以 Windows 为主要平台 |
| C. 纯批处理 / PowerShell 菜单 | 零依赖、最简单 | 非图形界面，不够小白 | 只是自己临时用 |

建议 **A** 起步；数据包结构（store 与源码）与选型无关，之后换 B 也不受影响。本机探测到 Python 可用；具体版本与打包链待确认后实施。

## 8. 硬性限制（关系到「给谁用」）

- **只能给 Windows 10/11 64 位**。原生二进制（codex、sharp、node-pty…）绑平台与 Node ABI；Mac/Linux 需另做数据包。
- **接收机需 Node 22.19+ 或 24+**——正对应步骤②（msi 静默安装需管理员权限，界面提前提示点【允许】）。
- 每台接收机**第一次**仍需本地 `pnpm run build`（5–15 分钟，不联网）。想连这步也省（二期）可把 build 产物也做成离线增量包，但要求接收机平台 / Node 与打包机一致，MVP 先不做。

## 9. 制作「离线安装盘」的产出物（打包机侧流程）

1. 本机 `pnpm install` 完整成功（✅ 2026-09-03 已完成）。
2. 用官方方式确认能 `pnpm dsh web` 启动（✅ 已确认，成功输出带 token 的 URL 并自动打开浏览器）。
3. 打包工具自动收集：源码快照（排除 .git / node_modules）＋ `pnpm store path` 目录 ＋ pnpm tgz ＋ 可选 node msi。
4. 生成安装盘（一个大 7z / 分卷包，或一个放好文件的目录结构，拷 U 盘 / 网盘均可）。
5. 在干净虚拟机里分别验收「在线模式」与「离线模式」全流程。

## 10. MVP 拆期（建议节奏）

- **阶段 0 ✅**：本机 install + build 均完整成功，store 已含全部包；`pnpm dsh web` 已能直接启动。
- **阶段 1**：最小核心（命令行阶段可接受）——离线与在线两种来源都能完成：检测 → 装依赖 → build → 启动；在第二台机器各自自验。
- **阶段 2**：图形界面（模式三选一、按钮、状态、中文日志、UAC 提示）。
- **阶段 3**：内置 node msi 静默安装、pnpm tgz 离线装、源码自动解压 / 自动 clone。
- **阶段 4**：打正式安装盘，虚拟机上验收在线与离线两模式。
- **二期**：build 产物离线增量包；Mac / Linux 支持。

## 11. 需要拍板的决策（P0 优先级最高）

- **P0 技术栈**：A(Python+Tkinter) / B(C#/.NET) / C(批处理菜单)？
- **P0 接收对象**：对方的电脑都 Windows 10/11 64 位吗？装没装过 Node？
- **P1 默认安装方式**：启动页默认「自动选择」可以吗？还是默认「离线」（有离线包时）？
- **P1 Node 安装包是否内置**：内置 = 对方免装 Node 但 +35MB 且弹管理员授权；不内置 = 对方先装 Node 或在线下载（多一步）。
- **P1 在线装依赖是否默认国内镜像 npmmirror**：官方源直连实测很慢；默认镜像可大幅提速（仍可选官方源）。
- **P1 数据包形态**：单个分卷 7z 传网盘 / U 盘，还是先做本地目录版自测？
- **P2 界面语言**：全中文？（默认全中文）
- **P2 开发顺序**：接受「先命令行最小版第二台机自验，再加图形界面」吗？

## 12. 文档不能替你决定的部分

- store 完整性已验证（本机 install 成功），但「离线安装在第二台机器 100% 可行」仍需阶段 1 实机验证。
- 接收机 `pnpm run build` 需要可写硬盘空间（node_modules + 产物可能合计再占 3–5GB）。

> 下一步：把这 8 个决策定了，我按「阶段 1 → 2 → 3 → 4」先做出能自用的最小版本，再逐步包图形界面与 node 安装包。
